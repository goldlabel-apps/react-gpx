
export const styles = theme => ({
    view: {
        margin: 'auto',
        maxWidth: 500,
    },
    card: {
        padding: theme.spacing(),
        margin: theme.spacing(),
        // border: '1px solid white',
    }
});
